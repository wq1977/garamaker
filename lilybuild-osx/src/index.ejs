<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>lilybuild-osx</title>
    <link rel="stylesheet" href="./static/bootstrap/dist/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="./static/@fortawesome/fontawesome/styles.css"/>    <% if (htmlWebpackPlugin.options.nodeModules) { %>
      <!-- Add `node_modules/` to global paths so `require` works properly in development -->
      <script>
        require('module').globalPaths.push('<%= htmlWebpackPlugin.options.nodeModules.replace(/\\/g, '\\\\') %>')
      </script>
    <% } %>
  </head>
  <body>
    <div id="app"></div>
    <!-- Set `__static` path to static files in production -->
    <% if (!process.browser) { %>
      <script>
        if (process.env.NODE_ENV !== 'development') window.__static = require('path').join(__dirname, '/static').replace(/\\/g, '\\\\')
      </script>
    <% } %>
  <script>
      window.jQuery = window.$ = require('jquery');
  </script>
  <script src="./static/bootstrap/dist/js/bootstrap.bundle.js"></script>
  <script src="./static/bootstrap-input-spinner/src/bootstrap-input-spinner.js"></script>
    <!-- webpack builds are automatically injected -->
  </body>
</html>
